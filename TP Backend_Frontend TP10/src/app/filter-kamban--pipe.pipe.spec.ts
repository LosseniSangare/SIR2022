import { FilterKambanPipePipe } from './filter-kamban--pipe.pipe';

describe('FilterKambanPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterKambanPipePipe();
    expect(pipe).toBeTruthy();
  });
});
