import { FilterFichePipePipe } from './filter-fiche--pipe.pipe';

describe('FilterFichePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFichePipePipe();
    expect(pipe).toBeTruthy();
  });
});
