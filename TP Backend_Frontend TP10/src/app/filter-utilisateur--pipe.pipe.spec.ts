import { FilterUtilisateurPipePipe } from './filter-utilisateur--pipe.pipe';

describe('FilterUtilisateurPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUtilisateurPipePipe();
    expect(pipe).toBeTruthy();
  });
});
